apiVersion: v1
kind: Service
metadata:
  name: old-world-realms-backend-service
  labels:
    app: old-world-realms-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: old-world-realms-backend

